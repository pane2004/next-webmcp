---
"next-webmcp": minor
---

Require Node 22 or newer (`engines.node >= 22`). Node 20 reached end of life in April 2026, and the
build tooling relies on Node's built-in TypeScript support. CI now runs on Node 22 and 24.
